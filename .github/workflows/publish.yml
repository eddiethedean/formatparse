name: Publish to PyPI

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  MATURIN_PROFILE: release

jobs:
  build-linux-wheels:
    name: Build Linux manylinux wheels (${{ matrix.architecture }} / py${{ matrix.python }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux manylinux2014 x86-64 (GLIBC 2.17+, compatible with most Linux systems)
          - python: "3.9"
            architecture: x86-64
            manylinux: "manylinux2014"
          - python: "3.10"
            architecture: x86-64
            manylinux: "manylinux2014"
          - python: "3.11"
            architecture: x86-64
            manylinux: "manylinux2014"
          - python: "3.12"
            architecture: x86-64
            manylinux: "manylinux2014"
          - python: "3.13"
            architecture: x86-64
            manylinux: "manylinux2014"
    container:
      image: quay.io/pypa/${{ matrix.manylinux }}_${{ matrix.architecture }}:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
          source $HOME/.cargo/env
          rustc --version

      - name: Install maturin
        run: |
          source $HOME/.cargo/env
          # Convert Python version (e.g., "3.9" -> "39") for manylinux paths
          PY_VER=$(echo "${{ matrix.python }}" | tr -d '.')
          # Python 3.9+ uses cp<VERSION>-cp<VERSION>, older versions may use cp<VERSION>-cp<VERSION>m
          if [ -f "/opt/python/cp${PY_VER}-cp${PY_VER}/bin/pip" ]; then
            /opt/python/cp${PY_VER}-cp${PY_VER}/bin/pip install maturin
          elif [ -f "/opt/python/cp${PY_VER}-cp${PY_VER}m/bin/pip" ]; then
            /opt/python/cp${PY_VER}-cp${PY_VER}m/bin/pip install maturin
          else
            echo "Error: Could not find Python ${{ matrix.python }} (cp${PY_VER}) in container"
            ls -la /opt/python/
            exit 1
          fi

      - name: Build wheel
        env:
          PYO3_USE_ABI3_FORWARD_COMPATIBILITY: ${{ matrix.python == '3.13' && '1' || '' }}
          PATH: $HOME/.cargo/bin:$PATH
        run: |
          source $HOME/.cargo/env
          # Convert Python version (e.g., "3.9" -> "39") for manylinux paths
          PY_VER=$(echo "${{ matrix.python }}" | tr -d '.')
          # Determine Python path
          if [ -f "/opt/python/cp${PY_VER}-cp${PY_VER}/bin/python" ]; then
            PYTHON="/opt/python/cp${PY_VER}-cp${PY_VER}/bin/python"
          elif [ -f "/opt/python/cp${PY_VER}-cp${PY_VER}m/bin/python" ]; then
            PYTHON="/opt/python/cp${PY_VER}-cp${PY_VER}m/bin/python"
          else
            echo "Error: Could not find Python ${{ matrix.python }} (cp${PY_VER}) in container"
            ls -la /opt/python/
            exit 1
          fi
          $PYTHON -m maturin build \
            --manifest-path formatparse-pyo3/Cargo.toml \
            --out dist \
            --strip \
            --release \
            --manylinux ${{ matrix.manylinux }}

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheel-linux-${{ matrix.architecture }}-py${{ matrix.python }}
          path: dist/*.whl
          if-no-files-found: error
          retention-days: 7

  build-wheels:
    name: Build wheels (${{ matrix.os }} / ${{ matrix.architecture }} / py${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS latest (typically arm64/Apple Silicon)
          - os: macos-latest
            python: "3.9"
            architecture: aarch64
          - os: macos-latest
            python: "3.10"
            architecture: aarch64
          - os: macos-latest
            python: "3.11"
            architecture: aarch64
          - os: macos-latest
            python: "3.12"
            architecture: aarch64
          - os: macos-latest
            python: "3.13"
            architecture: aarch64
          # Windows x86-64
          - os: windows-latest
            python: "3.9"
            architecture: x86-64
          - os: windows-latest
            python: "3.10"
            architecture: x86-64
          - os: windows-latest
            python: "3.11"
            architecture: x86-64
          - os: windows-latest
            python: "3.12"
            architecture: x86-64
          - os: windows-latest
            python: "3.13"
            architecture: x86-64
          # macOS 15 Intel (x86-64)
          - os: macos-15-intel
            python: "3.9"
            architecture: x86-64
          - os: macos-15-intel
            python: "3.10"
            architecture: x86-64
          - os: macos-15-intel
            python: "3.11"
            architecture: x86-64
          - os: macos-15-intel
            python: "3.12"
            architecture: x86-64
          - os: macos-15-intel
            python: "3.13"
            architecture: x86-64
          # Windows 11 ARM (aarch64) - Python 3.9 and 3.10 not available for Windows ARM
          - os: windows-11-arm
            python: "3.11"
            architecture: aarch64
          - os: windows-11-arm
            python: "3.12"
            architecture: aarch64
          - os: windows-11-arm
            python: "3.13"
            architecture: aarch64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install maturin

      - name: Build wheel
        env:
          PYO3_USE_ABI3_FORWARD_COMPATIBILITY: ${{ matrix.python == '3.13' && '1' || '' }}
        run: maturin build --manifest-path formatparse-pyo3/Cargo.toml --out dist --strip --release

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-${{ matrix.architecture }}-py${{ matrix.python }}
          path: dist/*.whl
          if-no-files-found: error
          retention-days: 7

  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install maturin

      - name: Build sdist
        run: maturin sdist --manifest-path formatparse-pyo3/Cargo.toml --out dist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  security-check:
    name: Security Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run cargo audit
        run: |
          cargo install cargo-audit --version 0.19.0 || cargo install cargo-audit
          cargo audit

      - name: Run cargo deny check
        run: |
          cargo install cargo-deny --version 0.14.3 || cargo install cargo-deny
          cargo deny check --config cargo-deny.toml

      - name: Run pip-audit
        run: |
          python -m pip install --upgrade pip pip-audit
          pip-audit --desc

  publish:
    name: Publish to PyPI
    needs: [build-linux-wheels, build-wheels, build-sdist, security-check]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # Required for trusted publishing
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          merge-multiple: true
          path: artifacts/wheels

      - name: Download sdist
        uses: actions/download-artifact@v4
        with:
          pattern: sdist
          merge-multiple: true
          path: artifacts/sdist

      - name: Combine artifacts
        shell: bash
        run: |
          set -e
          
          # Debug: Show what we downloaded
          echo "=== Artifacts structure ==="
          find artifacts -type f -o -type d 2>/dev/null | sort || true
          echo ""
          
          # Create clean dist directory
          mkdir -p dist
          
          # Copy wheels from artifacts directory (handle all possible structures)
          echo "=== Copying wheels ==="
          find artifacts -name "*.whl" -type f -exec cp -v {} dist/ \; || true
          
          # Copy sdist from artifacts directory (handle all possible structures)
          echo "=== Copying sdist ==="
          find artifacts -name "*.tar.gz" -type f -exec cp -v {} dist/ \; || true
          
          # Verify we have files
          echo ""
          echo "=== Final dist contents ==="
          ls -lah dist/ || echo "dist/ is empty or doesn't exist"
          echo ""
          echo "=== Distribution files ==="
          find dist -type f \( -name "*.whl" -o -name "*.tar.gz" \) 2>/dev/null | sort || echo "No files found"
          echo ""
          
          # Count files
          WHEEL_COUNT=$(find dist -name "*.whl" -type f 2>/dev/null | wc -l || echo "0")
          SDIST_COUNT=$(find dist -name "*.tar.gz" -type f 2>/dev/null | wc -l || echo "0")
          echo "Wheels: $WHEEL_COUNT"
          echo "Source distributions: $SDIST_COUNT"
          
          if [ "$WHEEL_COUNT" -eq 0 ] && [ "$SDIST_COUNT" -eq 0 ]; then
            echo "ERROR: No distribution files found in dist/!"
            echo "Checking artifacts directory:"
            find artifacts -type f 2>/dev/null || true
            exit 1
          fi
          
          echo "✅ Successfully prepared $((WHEEL_COUNT + SDIST_COUNT)) distribution files"

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          password: ${{ secrets.PYPI_API_TOKEN }}

